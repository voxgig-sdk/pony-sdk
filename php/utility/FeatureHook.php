<?php
declare(strict_types=1);

// Pony SDK utility: feature_hook

class PonyFeatureHook
{
    public static function call(PonyContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
